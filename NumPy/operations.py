import numpy as np

arr = np.arange(0, 11)

hundred = 1 / arr # Also * - + and so on

for i in arr:
    print(i)

print(np.sqrt(arr))
print(np.exp(arr))
print(np.exp2(arr))
print(np.min(arr))
print(arr.max())
print(np.sin(arr))
